[project]
name = "python-module"
version = "0.1.0"
description = "Example of a packaged Python module with tests"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.7.0,<0.9.0"]
build-backend = "uv_build"
