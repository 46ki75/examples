{
  "name": "node-lambda-graphql",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prebuild": "rm -rf dist/*",
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=dist/index.mjs --format=esm --tsconfig=tsconfig.json",
    "postbuild": "cp graphiql.html dist/graphiql.html && cp schema.graphql dist/schema.graphql && cd dist && zip -r lambda.zip ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@graphql-tools/schema": "^10.0.10",
    "graphql": "^16.9.0"
  }
}